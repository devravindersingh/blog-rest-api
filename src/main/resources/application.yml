spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/myblog
    username: ravinder
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
  security:
    user:
      name: ravinder
      password: root
      roles:
        - ADMIN